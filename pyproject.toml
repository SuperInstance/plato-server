[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "plato-server"
version = "1.0.0"
description = "Cocapn Fleet module — plato-server"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
keywords = ["cocapn", "fleet"]

[project.scripts]
plato_server = "test_server:main"
